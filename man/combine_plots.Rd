% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualizations.R
\name{combine_plots}
\alias{combine_plots}
\title{Combine Map and Bar Chart}
\usage{
combine_plots(
  map_plot,
  bar_plot,
  map_width = 0.75,
  bar_position = list(x = 0.6, y = 0.3, width = 0.28, height = 0.42)
)
}
\arguments{
\item{map_plot}{ggplot2 map object (from create_map())}

\item{bar_plot}{ggplot2 bar chart object (from create_bar_plot())}

\item{map_width}{Proportion of width for map (default: 0.75)}

\item{bar_position}{List with x, y, width, height for bar chart placement}
}
\value{
ggplot2 object with combined plots
}
\description{
Creates a combined visualization with accessibility map and summary bar chart.
}
\examples{
\dontrun{
# Create individual plots
map <- create_map(access_dt, points, adm0, adm1, cats)
bar <- create_bar_plot(summary_stats, cats)

# Combine plots
combined <- combine_plots(map, bar)
print(combined)

# Custom positioning
combined <- combine_plots(
  map, bar,
  bar_position = list(x = 0.7, y = 0.4, width = 0.25, height = 0.35)
)
}

}
